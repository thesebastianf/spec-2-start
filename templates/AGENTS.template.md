# {{PROJECT_NAME}} Agents

This project uses the following AI agents:

{{AGENTS_TABLE}}

## How it works

Each agent has:
- **Accountability** — Clear ownership of a domain
- **Responsibilities** — What the agent does
- **Instructions** — Rules the agent follows
- **applyTo** — File patterns the agent operates on

The **Orchestrator** coordinates all agents and delegates tasks.

## Agent Files

Agent definitions are in `.github/agents/`:

{{AGENT_FILES_LIST}}
